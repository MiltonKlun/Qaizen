{
  "name": "STORY-002 — reqres.in",
  "values": [
    {
      "key": "base_url",
      "value": "https://reqres.in/api",
      "enabled": true
    },
    {
      "key": "api_key",
      "value": "",
      "enabled": true,
      "_comment": "Intentionally empty in the committed file — never store the secret here. scripts/run-newman.js injects the real value from the REQRES_API_KEY env var at run time via newman --env-var 'api_key=...'."
    }
  ]
}
