# Planner Brief — SK-13: As a user I want to sort products by name and price

## Story summary

On the Saucedemo inventory page, the product sort dropdown orders products by
name (A→Z, Z→A) and price (low→high, high→low), and the chosen option stays
visible as the active selection. Sorting is client-side; there is no backend
sort API, so this story is E2E-only (confirmed at Gate 1).

## Acceptance criteria

0. sorting A to Z works correctly
1. sorting Z to A works correctly
2. sorting low to high works correctly
3. sorting high to low works correctly
4. selected option remains visible

## Risks (anchors)

- **RISK-001** (high): a selected sort produces the wrong order. Related ACs:
  [0, 1, 2, 3].
- **RISK-002** (medium): price sorted as strings ($10 before $9) rather than
  numbers. Related ACs: [2, 3].
- **RISK-003** (low): the control doesn't show the active selection. Related
  AC: [4].

## In-scope scenarios for the Playwright Planner

- **Name A→Z** (TC-001, RISK-001): select the option, assert names ascending.
- **Name Z→A** (TC-002, RISK-001): select the option, assert names descending.
- **Price low→high** (TC-003, RISK-001/002): select the option, assert prices
  ascending — parse prices as NUMBERS, not strings.
- **Price high→low** (TC-004, RISK-001/002): select the option, assert prices
  descending numerically.
- **Selected option visible** (TC-005, RISK-003): after selecting a non-default
  option, assert the control reflects it as the active selection.

All five share the same precondition (logged-in inventory page) and the same
interaction (choose a dropdown option); they differ in the assertion.

## Out-of-scope for the Planner

- Login itself (covered by SK-10) beyond reaching the inventory page as setup.
- Cart / checkout / logout (other stories).
- Any API-level assertion: Saucedemo sorts client-side; no endpoint exists.
- Visual styling of the dropdown or product cards.
- Performance of sorting.

## UI baseline notes

- Saucedemo sort control: `[data-test="product-sort-container"]` (a `<select>`)
  with option values `az`, `za`, `lohi`, `hilo`. Product names render as
  `[data-test="inventory-item-name"]`; prices as
  `[data-test="inventory-item-price"]` (format like `$29.99`). CONFIRM these
  live via the playwright-test MCP during planning (§3.8 — don't assume).
- To get the inventory page, log in as standard_user / secret_sauce first.

## Ambiguities still open

- "works correctly" = the visible product order matches the selected ordering
  (names alphabetical asc/desc; prices numeric asc/desc) — confirmed at Gate 1.
- E2E-only scope confirmed at Gate 1 (no backend sort API).

## Traceability

- Story: SK-13 (Jira)
- Risks covered: RISK-001, RISK-002, RISK-003
- Test cases: TC-001, TC-002, TC-003, TC-004, TC-005
