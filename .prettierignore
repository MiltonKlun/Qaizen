node_modules/
reports/
playwright-report/
test-results/
traces/
scratchpad/
.claude/
package-lock.json

# Scaffolded by `npx playwright init-agents` — do not auto-format
.mcp.json

# Runtime cache from the playwright-test MCP probes — never authored content
.playwright-mcp/

# Authoritative planning and instruction docs — owned by the team, not auto-formatted
CLAUDE.md
README.md
IMPROVEMENT-PLAN-2.md

# Archived runs are evidence: byte-identical to what each run produced, never
# reformatted (scripts/lib/run-lifecycle.js, task group 4.1).
runs/
# Transient runner state (the new-story transition record and staging).
.qaizen/
